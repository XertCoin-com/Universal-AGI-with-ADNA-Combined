# Edge / Jetson Guide (Prototype)

- Install CUDA / JetPack per device
- Run `python node/agent.py --model tiny-llm --task benchmark/mnist.json --offline`
- Configure MIG (if supported) to host multiple agents concurrently
